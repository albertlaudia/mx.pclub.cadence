# packages/ui

Shared design system. **Phase 1 — not yet scaffolded.**

When this is built, it will be:
- A shared package with Flutter widgets + React components
- The home screen, movement screen, chapter mark, Section UI
- RTL-ready from day one
- Accessible (WCAG 2.1 AA minimum)

Pattern from `mx.pclub.caloriecounter` and `mx.pclub.pulse`.
