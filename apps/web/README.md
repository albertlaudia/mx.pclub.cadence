# apps/web

Next.js app: marketing site + web app. **Phase 1 — not yet scaffolded.**

When this is built, it will follow the pattern from `mx.pclub.web`:
- Next.js 15 with App Router
- Marketing site (SEO for "daily devotional", "prayer app", etc.)
- Web app for users on a laptop
- Firebase Auth + Firestore
- Shared design system from `packages/ui`

See `docs/ARCHITECTURE.md` for the system that this app delivers.
